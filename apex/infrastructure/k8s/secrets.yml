apiVersion: v1
kind: Secret
metadata:
  name: apex-secrets
  namespace: apex-fleet
type: Opaque
stringData:
  # Database
  DATABASE_URL: "postgresql://apex:apex_secure_2024@postgres:5432/apex_fleet"
  
  # Redis
  REDIS_URL: "redis://redis:6379"
  
  # Object Storage (MinIO or S3)
  S3_ENDPOINT: "http://minio:9000"
  S3_ACCESS_KEY: "apex_admin"
  S3_SECRET_KEY: "apex_secure_2024"
  S3_BUCKET: "apex-artifacts"
  
  # Git credentials (for accessing repos)
  GITHUB_TOKEN: ""
  
  # API Keys (if using external LLM providers)
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  
  # Monitoring
  GRAFANA_ADMIN_PASSWORD: "apex_dashboard_2024"